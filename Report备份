# MiniLisp-RS 项目报告初稿

## 1. 项目概述

Mini-Lisp 是对 Lisp 语言的一种简化实现形式，保留了 S 表达式、符号求值、函数式调用、闭包和列表处理等核心特征。本项目以 Rust 为实现语言，完成了一个轻量级 Mini-Lisp 解释器，并在命令行解释执行的基础上扩展了图形界面。

我们曾在软件设计与实现课程中，使用C++实现了一个类似的 Mini-Lisp 解释器，在当时体验到了大量的面向对象编程的思想以及复杂的内存管理问题。因此，我们产生了用 Rust 实现一个新版本的想法，期望借此更深刻地体会 Rust 语言的特性。
故而，本项目的主要目标包括两个方面：第一，实现一个能够正确解析并求值 Mini-Lisp 表达式的解释器，支持基本算术、条件判断、变量定义、匿名函数、列表操作和若干内置过程。第二，在此前使用 C++ 实现类似解释器的经验基础上，尝试使用 Rust 重新设计该项目，从而体会 Rust 在所有权、模式匹配、错误处理和模块化组织方面的特点。第三，我们希望在解释器核心之外提供更友好的使用方式，即完成解释器的桌面图形界面。

从最终结果来看，项目已经形成了较完整的系统：既可以通过 REPL 逐行输入 Lisp 表达式，也可以直接执行 `.scm` 文件；图形界面支持输入区、输出区、按钮控制、语法高亮以及括号匹配提示等功能，使得 Mini-Lisp 的演示和调试更直观。

## 2. 项目文件结构

项目的核心语言逻辑位于 `src/` 目录下，图形界面入口位于 `src/bin/` 下。其主要文件结构如下：

```text
src/
├── lib.rs             对外库入口，提供统一执行接口
├── main.rs            命令行入口，负责 REPL 与文件执行
├── token.rs           Token 定义
├── tokenizer.rs       词法分析器
├── parser.rs          语法分析器
├── value.rs           运行时值的表示
├── eval_env.rs        求值环境与求值器
├── form.rs            特殊形式实现
├── builtins.rs        内置过程实现
├── output.rs          输出抽象
└── error.rs           错误类型定义和错误打印

src/bin/
└── gui.rs             GUI 程序入口

src/bin/gui/
├── theme.rs           GUI 主题、颜色、字号、尺寸配置
└── editor.rs          编辑器高亮与装饰逻辑
```

## 3. 使用方法与可用功能

### 3.1 命令行使用方式

项目支持以命令行形式启动交互式 REPL：

```bash
cargo run --bin mini_lisp_rs
```
按 Ctrl+Z 退出。

也支持直接执行一个 Lisp 源文件：

```bash
cargo run --bin mini_lisp_rs sort_test1.scm
```
(sort_test1.scm是我们提供的排序算法示例文件)

在命令行模式下，用户可以逐条输入表达式并立即查看求值结果，这对于调试解释器行为、验证内置函数和测试递归过程都很方便。

### 3.2 图形界面使用方式

除了命令行模式外，项目还实现了一个独立的桌面图形界面：

```bash
cargo run --release --bin gui
```

构建完成后，也可以直接点击运行生成的可执行文件，不再依赖 PowerShell：

```text
target/release/gui.exe
```

这一点提升了项目的可用性和演示效果。

### 3.3 当前支持的语言功能

目前解释器已经支持以下功能：

- 数值与布尔值的表示和运算
- 符号求值与变量绑定
- `define`、`lambda`、`if`、`begin`、`cond`、`let` 等特殊形式
- 列表、序对以及基础列表处理过程
- `car`、`cdr`、`cons`、`append` 等基础内置过程
- `map`、`filter`、`reduce` 等高阶函数
- 文件输入与批量执行

### 3.4 图形界面的辅助功能

在图形界面部分，项目实现了以下提升体验的编辑器功能：

- 输入框和输出框左右分栏显示，便于对照查看
- 一键执行(Run)、清空输出(Clear Output)、重置环境(Reset Env)
- 查看示例代码和已实现的语言功能（包括特殊形式(Special Forms)和内置过程(Built-in Procedures)）的按钮(Reference)
- 输入文本的自动语法着色
- 括号匹配提示

## 4. 系统设计与实现细节

### 4.1 总体设计思路

系统整体采用分层设计。最底层是词法分析与语法分析，中间层是运行时值表示和求值环境，最上层是命令行和图形界面两种输入输出前端。用户输入一段 Lisp 代码后，系统首先将源码拆分为 token，再将 token 解析为内部值结构，最后在求值环境中执行并返回结果。

这一设计的关键优点在于职责清晰。词法分析器只关心字符如何划分为记号；语法分析器只关心 token 如何构成合法的 S 表达式；求值器则只关注表达式在环境中的含义；CLI 和 GUI 则只负责向用户组织输入输出。由于模块边界明确，在后续新增功能时可以较容易定位修改位置。

### 4.2 词法分析与语法分析

词法分析器 `tokenizer.rs` 的主要任务是将原始字符串转换为一系列 token，例如左括号、右括号、数字、字符串、符号等。Mini-Lisp 的语法本身并不复杂，但因为它高度依赖括号结构和前缀形式，所以分词阶段的准确性十分关键。

语法分析器 `parser.rs` 在 token 流的基础上递归构造内部表达式结构。由于 Lisp 使用 S 表达式表示程序，语法树与数据结构天然接近，因此解析逻辑相比一些中缀表达式语言更直接。解析器最终将输入组织为统一的 `Value` 表示，为后续求值阶段提供结构化输入。

### 4.3 运行时值表示

运行时值由 `value.rs` 中的枚举类型统一表示，例如布尔值、数值、字符串、符号、空值、序对、内置过程和用户定义过程等。这种设计使得解释器可以在一个统一的类型系统中处理所有 Lisp 对象。

在具体实现上，项目使用了 Rust 的 `enum` 来表达值的不同种类，而不是采用传统面向对象中的继承层次。这样做的好处是结构集中、模式明确，后续在求值、打印和类型检查时可以通过 `match` 对所有可能情况进行穷尽式处理，减少遗漏分支的风险。

### 4.4 求值环境与闭包

`eval_env.rs` 是整个解释器的核心模块之一。环境对象负责维护符号到值的映射关系，并通过父环境指针形成链式作用域结构。表达式求值时，解释器会根据当前作用域查找变量，并在必要时创建子环境。

用户定义函数 `lambda` 的实现不仅需要保存参数列表和函数体，还需要保存函数定义时所在的环境。这一点对于闭包语义十分重要。项目最终采用显式保存定义时环境的方式，使得函数在调用时能够基于定义环境而不是调用环境构造新的局部作用域，从而实现正确的词法作用域。

这部分是解释器语义正确性的关键。如果只复制当前符号表而不保留环境链，就很容易出现看似可以运行、但并不符合闭包语义的实现。经过不断调整后，项目中的 `lambda` 已经能够较好地支持高阶函数和作用域嵌套。

### 4.5 特殊表单与内置过程

在 Lisp 中，并不是所有表达式都遵循“先求值参数，再调用函数”的统一规则，例如 `define`、`if`、`lambda`、`begin`、`cond` 和 `let` 都具有自己的求值语义。因此，项目将这类语法结构单独放入 `form.rs` 中实现，以便与普通函数调用区分开来。

内置过程则集中实现于 `builtins.rs` 中，主要包括算术运算、比较运算、列表操作以及若干高阶函数。将特殊表单和内置过程分离，一方面使代码结构更清晰，另一方面也有助于理解 Lisp 中“语法形式”和“普通过程”的本质差异。

### 4.6 输入输出与 GUI 设计

命令行与图形界面共用解释器核心，但在输入输出层面采用不同封装。CLI 强调快速交互和文件执行，GUI 则强调可视化和编辑体验。为了保证两者行为一致，项目中将部分输出逻辑抽象在 `output.rs` 中，使不同前端都能复用相近的结果展示机制。

图形界面采用 `eframe/egui` 实现，并进一步拆分为入口、主题和编辑器三个模块。入口模块负责界面布局、按钮事件和解释器调用；主题模块负责颜色、字号、间距等可调参数；编辑器模块负责语法高亮、当前行高亮和括号匹配高亮。这样的拆分使界面部分既具备可扩展性，也便于针对某一类视觉问题进行局部修改。

## 5. Rust 使用特点与 C++ 对比分析

### 5.1 内存管理：从默认共享到显式建模

本项目中最直接的语言差异，首先体现在值对象和环境对象的生命周期管理上。在 C++ 风格实现中，这类解释器通常很自然地大量使用 `std::shared_ptr`，因为表达式节点、环境链和闭包对象都具有共享引用的特性。这种方式写起来直接，但会将很多对象关系的正确性问题留到运行期处理。

Rust 的实现虽然最终同样使用了引用计数，但它要求开发者更明确地区分“共享所有权”和“内部可变性”。在本项目中，值对象使用 `Rc<Value>` 表示，共享环境使用 `Rc<RefCell<EvalEnv>>` 表示。这种做法使得“为什么要共享”和“为什么需要可变”成为两个被明确表达的设计决策，而不是默认打包在同一种智能指针里。

这种差异虽然提高了实现门槛，但也让环境别名关系、闭包捕获方式以及局部修改行为都更加清晰。对于解释器这类拥有复杂对象图的程序而言，这种清晰性具有长远价值。

### 5.2 数据表示：从继承层次到枚举匹配

如果用 C++ 实现 `Value`，一个常见思路是定义抽象基类，再派生出数值、字符串、列表、函数等不同子类。这符合传统面向对象设计，但对于解释器而言，绝大多数操作其实不是“让对象自己多态地完成工作”，而是“根据值的种类执行对应语义”。

Rust 的 `enum` 更适合这种封闭值集合的建模方式。借助 `match`，解释器能够以非常直接的形式处理所有值类型，并在编译期检查是否遗漏某个分支。相比之下，C++ 中如果采用类型转换或虚函数混合实现，某些遗漏往往要到运行期才会暴露。

因此，在这个项目中，Rust 并不是单纯换了一种语法，而是推动了值表示方式的整体变化。这种变化反过来又影响了解析器、求值器和输出格式化函数的写法。

### 5.3 环境与闭包：从能运行到语义正确

闭包实现是本次项目里最值得比较的部分之一。一个“表面上能运行”的解释器实现，往往只要在函数调用时复制当前环境，再将实参绑定到形参即可。然而这样的做法并不一定真正符合词法作用域。

在本项目中，Rust 的实现最终选择在 `lambda` 值中显式保存定义时环境，并在调用时基于该环境构造局部子环境。这一点使闭包的行为与语言理论保持一致，特别是在高阶函数、嵌套定义和 `let` 转换场景下更为重要。

与 C++ 相比，Rust 并没有自动替开发者实现正确语义，但它通过所有权和类型建模的压力，迫使开发者尽早正视“闭包到底保存了什么”这一问题。也正因为如此，最终得到的实现往往比一开始仅追求运行通过的版本更扎实。

### 5.4 错误处理：从异常式思维到结果类型思维

C++ 中的解释器错误处理常见两种策略：一是返回特殊值或空指针，二是直接抛出异常。前者的问题是错误传播容易变得隐式，后者则可能让普通控制流和异常控制流交织在一起。

Rust 的 `Result<T, E>` 机制要求错误处理直接体现在函数签名中。本项目中的求值核心大量采用 `Result<ValuePtr, LispError>` 返回值，使语法错误、运行时错误以及类似退出解释器这样的控制流都能够在类型层面被统一描述。

这种方式在代码书写初期会显得更繁琐，因为许多位置都需要显式传播错误；但从可维护性角度看，它让错误的来源、传播路径和最终处理位置都更加清楚，也降低了隐藏式失败的风险。

### 5.5 编译期约束带来的实现心智变化

从开发体验上看，Rust 并不总是比 C++ 更轻松。很多在 C++ 中可以快速写出的结构，到 Rust 中会受到借用规则、生命周期和可变性限制的约束，需要不断调整接口设计。例如，某个对象是按值传递还是共享引用，某个函数是否应该接收环境参数，某个数据结构是否需要内部可变性，这些问题往往不能拖到最后再处理。

但正是这种“在编译期把问题逼出来”的机制，让解释器设计中的一些模糊地带不得不被澄清。换言之，Rust 并没有消灭复杂性，而是把复杂性更早地暴露出来。这种特征在教学型项目中尤其有意义，因为它帮助开发者真正理解语言实现中的语义边界，而不仅仅是把代码写到“能跑”为止。

## 6. 结果展示

从功能完成度来看，本项目已经能够支持 Mini-Lisp 的基本解释执行，并提供命令行与图形界面两种交互模式。用户既可以在 REPL 中逐条测试表达式，也可以直接运行脚本文件，或者通过图形界面进行更直观的输入与观察。

以下是一些典型示例：

```lisp
(define x 10)
(define y 32)
(+ x y)
```

输出结果应为：

```text
42
```

再例如：

```lisp
(map (lambda (x) (* x x)) '(1 2 3 4))
```

输出结果应为：

```text
(1 4 9 16)
```

在 GUI 中，这些代码可以直接输入到左侧编辑区并执行，右侧输出区会显示运行结果；同时输入框支持自动着色、当前行高亮与括号匹配高亮，有助于提升输入可读性和调试效率。

## 7. 总结

### 7.1 项目亮点与不足

本项目的主要亮点在于：
- 较完整地实现了一个教学型 Mini-Lisp 解释器，覆盖了解析、求值、闭包、特殊表单和高阶函数等关键内容。
- 解释器核心之外实现了独立桌面 GUI，并对界面进行了模块化拆分和可配置化设计。
- 在实现过程中充分体现了 Rust 与 C++ 不同的工程思维方式，为后续语言实现课程学习提供了比较样本。

当然，项目仍有进一步完善空间：
- 目前 GUI 仍以轻量级编辑器体验为主，尚未加入更复杂的调试辅助能力。
- 解释器支持的语言特性也仍然是 Mini-Lisp 级别，没有涉及宏系统以及更完整的标准库。
- 此外，部分文档和注释仍可以进一步细化，以便未来其他开发者更快理解代码组织方式。

### 7.2 分工说明

本次项目的分工如下：

- 周静怡：负责项目初始框架搭建，完成早期词法分析器、语法分析框架的建立；参与后续解释器功能扩展，包括 `lambda` 求值等部分特殊形式，以及添加具体内置过程；完善文件输入功能；完成 GUI 界面实现，加入自动着色等功能；参与报告撰写。
- 王嘉怡：负责语法分析器的完善与接口调整，完成解释器求值、变量定义与查找、调用内置过程等功能；进行解释器调试测试与 bug 修复；完成 GUI 功能优化，包括输入清除、示例代码和语言规则查看功能。

本项目的全部代码可见https://github.com/Janezzzhou/MiniLisp-RS